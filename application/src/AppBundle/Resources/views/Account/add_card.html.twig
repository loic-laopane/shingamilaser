{% extends "AppBundle:Account:account.layout.html.twig" %}

{% block title %}{{ parent() }} > {{ 'Add card' | trans }}{% endblock %}

{% block content %}
    <h1>{{ 'Add card' | trans }}</h1>
    <legend></legend>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary btn-fill">{{ 'btn.attach' | trans }}</button>
    {{ form_end(form) }}
{% endblock %}

