{% extends "AppBundle:Account:account.layout.html.twig" %}

{% block title %}{{ parent() }} > {{ 'Edit profile' | trans }}{% endblock %}

{% block content %}
    <h1>{{ 'Edit profile' | trans }}</h1>
    <legend></legend>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary btn-fill">{{ 'btn.save' | trans }}</button>
    {{ form_end(form) }}
{% endblock %}
