{% extends "::base.html.twig" %}

{% block title %}{{ 'title.purchase.create' | trans }} :: {{ parent() }}{% endblock %}

{% block body %}
<h1>{{ 'title.purchase.create' | trans }}</h1>
    <div class="jumbotron">
        {{ 'jumbotron.purchase.create' | trans }}
    </div>
    <div class="panel panel-dark">
        <div class="panel-body">
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-primary btn-fill" formnovalidate><i class="fa fa-save"></i> {{ 'btn.create' | trans }}</button>
    <a class="btn btn-default btn-fill" href="{{ path('purchase_list') }}"><i class="fa fa-arrow-left"></i> {{ 'btn.back' | trans }}</a>
    {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
