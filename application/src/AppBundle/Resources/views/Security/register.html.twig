{% extends "::base.html.twig" %}

{% block title %}{{ 'registration' | trans }} :: {{ parent() }}{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-md-12">
            <div class="my-panel">
                <div class="heading">{{ 'Registration' | trans | title }}</div>
                <div class="body">
                    {{ form_start(form) }}
                    {{ form_errors(form) }}
                    <section>
                        {{ form_row(form.firstname) }}
                        {{ form_row(form.lastname) }}
                        {{ form_row(form.nickname) }}
                        {{ form_row(form.society) }}
                        {{ form_row(form.address) }}
                        {{ form_row(form.zipcode) }}
                        {{ form_row(form.city) }}
                        {{ form_row(form.birthdate) }}
                    </section>
                    <section>
                        {{ form_row(form.user) }}
                    </section>
                    <section>
                        <button type="submit" class="btn btn-primary btn-fill" formnovalidate><i class="fa fa-arrow-circle-o-right"></i> {{ 'btn.register' | trans }}</button>
                    </section>
                    {{ form_rest(form) }}
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>

{% endblock %}